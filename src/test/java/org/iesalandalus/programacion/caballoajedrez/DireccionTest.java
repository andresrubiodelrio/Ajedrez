package org.iesalandalus.programacion.caballoajedrez;

import static org.junit.Assert.*;
import org.junit.Test;

public class DireccionTest {

	/*@Test
	public void direccionesValidasTest() {
		//Pasará el test si hemos nombrado bien las direcciones
		Direccion direccion;
		direccion = Direccion.ARRIBA_DERECHA;
		assertEquals(Direccion.ARRIBA_DERECHA, direccion);
		direccion = Direccion.ARRIBA_IZQUIERDA;
		assertEquals(Direccion.ARRIBA_IZQUIERDA, direccion);
		direccion = Direccion.DERECHA_ARRIBA;
		assertEquals(Direccion.DERECHA_ARRIBA, direccion);
		direccion = Direccion.DERECHA_ABAJO;
		assertEquals(Direccion.DERECHA_ABAJO, direccion);
		direccion = Direccion.ABAJO_DERECHA;
		assertEquals(Direccion.ABAJO_DERECHA, direccion);
		direccion = Direccion.ABAJO_IZQUIERDA;
		assertEquals(Direccion.ABAJO_IZQUIERDA, direccion);
		direccion = Direccion.IZQUIERDA_ARRIBA;
		assertEquals(Direccion.IZQUIERDA_ARRIBA, direccion);
		direccion = Direccion.IZQUIERDA_ABAJO;
		assertEquals(Direccion.IZQUIERDA_ABAJO, direccion);
	}*/

}
